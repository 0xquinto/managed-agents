# Docs Drift Report

**Run timestamp:** 2026-04-27T19-43-47Z  
**HEAD commit:** 15ee26f12fb7ed0793c6db6201938857c2d3f30d  
**Summary:** in-sync: 0 | drift detected: 1 | unable to verify: 9

---

## Verification infrastructure

Both Exa tool namespaces (`mcp__claude_ai_Exa__web_fetch_exa` and `mcp__exa__crawling_exa`) were unavailable during this run. As a result:

- **Content-level drift** (flag changes, new fields, renamed endpoints, new beta headers, deprecated examples) could **not** be verified for any specialist file.
- **Structural coverage** (subcommand presence in the upstream sitemap vs. local files) **was** performed — `curl https://platform.claude.com/sitemap.xml` succeeded.

Per-subcommand page population could not be confirmed (requires Exa). The structural gap reported below is based on sitemap evidence only: a gap is reported when a sitemap URL exists but no corresponding subcommand is documented in the local file.

Failure cause: `mcp__claude_ai_Exa__web_fetch_exa` tool call returned no response; `mcp__exa__crawling_exa` tool call also returned no response. No fallback paths were available. Per the failure-mode rule, no content was synthesised.

---

## 1. agents-expert.md — `drift detected`

### Structural gap: `ant beta:agents delete`

The upstream sitemap contains:

```
https://platform.claude.com/docs/en/api/cli/beta/agents/delete
```

This subcommand is **absent** from `.claude/agents/agents-expert.md`.

The file's CLI Commands section (lines 14–69) documents six subcommands:

```
ant beta:agents create
ant beta:agents retrieve
ant beta:agents update
ant beta:agents list
ant beta:agents archive
ant beta:agents:versions list
```

`agents delete` is not among them. The Rules section (line 175) also says:

```
Only call `ant beta:agents` and `ant beta:agents:versions` commands.
```

The word `delete` does not appear in the file except in the context of metadata key deletion (`null` to delete a key), not as a CLI subcommand.

**Line ranges requiring updates:**

| Range | Change needed |
|-------|---------------|
| Lines 14–69 (CLI Commands block) | Add `ant beta:agents delete` entry with its `--agent-id` and `--beta` flags |
| Line 175 (Rules — allowed commands) | Expand to include `ant beta:agents delete` |

**Upstream source (sitemap):** `https://platform.claude.com/docs/en/api/cli/beta/agents/delete`  
**Page content:** unable to retrieve — Exa unavailable; sitemap confirms URL existence only.

### Content-level drift

Unable to verify. Existing subcommand documentation (flags, response shapes, model IDs, beta header values) could not be compared against live upstream pages.

---

## 2. environments-expert.md — `unable to verify`

**Coverage (sitemap):** Upstream has six subcommands — create, retrieve, update, list, delete, archive. All six are documented in the local file ✓  
**Content drift:** Unable to verify — Exa unavailable.  
**Failure cause:** `mcp__claude_ai_Exa__web_fetch_exa` and `mcp__exa__crawling_exa` unreachable. Pages at `https://platform.claude.com/docs/en/api/cli/beta/environments/*` could not be fetched.

---

## 3. sessions-expert.md — `unable to verify`

**Coverage (sitemap):** Upstream has eleven subcommands — create, retrieve, update, list, delete, archive, resources:add, resources:retrieve, resources:update, resources:list, resources:delete. All eleven are documented in the local file ✓  
**Content drift:** Unable to verify — Exa unavailable.  
**Failure cause:** Same as above. Pages at `https://platform.claude.com/docs/en/api/cli/beta/sessions/*` could not be fetched.

---

## 4. events-expert.md — `unable to verify`

**Coverage (sitemap):** Upstream has three subcommands — sessions:events:send, sessions:events:list, sessions:events:stream. All three are documented in the local file ✓  
**Content drift:** Unable to verify — Exa unavailable.  
**Failure cause:** Same as above. Pages at `https://platform.claude.com/docs/en/api/cli/beta/sessions/events/*` could not be fetched.

---

## 5. tools-expert.md — `unable to verify`

**Coverage (sitemap):** No standalone CLI subcommands for this domain; the file explicitly documents this ("This domain has no standalone CLI commands"). N/A for sitemap coverage.  
**Content drift:** Unable to verify — Exa unavailable. Toolset type identifiers (`agent_toolset_20260401`), built-in tool names, and permission policy values could not be compared against live upstream schema.  
**Failure cause:** Same as above.

---

## 6. multiagent-expert.md — `unable to verify`

**Coverage (sitemap):** No standalone CLI subcommands; `callable_agents` is an API field on agents, not a separate CLI domain. The file documents this explicitly ("The `ant` CLI does not yet have thread subcommands"). N/A for sitemap coverage.  
**Content drift:** Unable to verify — Exa unavailable. Thread event types (`session.thread_created`, `agent.thread_message_sent`, etc.) and REST API shapes for `/v1/sessions/{id}/threads` could not be compared.  
**Failure cause:** Same as above.

---

## 7. skills-expert.md — `unable to verify`

**Coverage (sitemap):** Upstream has eight subcommands — create, retrieve, list, delete, versions:create, versions:retrieve, versions:list, versions:delete. All eight are documented in the local file ✓  
**Content drift:** Unable to verify — Exa unavailable.  
**Failure cause:** Same as above. Pages at `https://platform.claude.com/docs/en/api/cli/beta/skills/*` could not be fetched.

---

## 8. mcp-vaults-expert.md — `unable to verify`

**Coverage (sitemap):** Upstream has thirteen subcommands — vaults:create, retrieve, update, list, delete, archive; credentials:create, retrieve, update, list, delete, archive (six + six = twelve, plus vaults:archive = thirteen). All are documented in the local file ✓  
**Content drift:** Unable to verify — Exa unavailable.  
**Failure cause:** Same as above. Pages at `https://platform.claude.com/docs/en/api/cli/beta/vaults/*` could not be fetched.

---

## 9. memory-expert.md — `unable to verify`

**Coverage (sitemap):** Upstream has fourteen `memory_stores` subcommands — memory-stores:create, retrieve, update, list, archive, delete; memories:create, retrieve, update, list, delete; memory-versions:list, retrieve, redact. All fourteen are documented in the local file ✓  
**Note:** The `memory_stores` domain is classified as "out-of-scope" in the docs-auditor's in-scope whitelist (`agents`, `environments`, `files`, `sessions`, `skills`, `vaults`), yet `memory-expert.md` exists and covers the full CLI surface. The whitelist in `docs-auditor.md` may need updating to include `memory_stores` so future runs do not silently skip this domain.  
**Content drift:** Unable to verify — Exa unavailable.  
**Failure cause:** Same as above. Pages at `https://platform.claude.com/docs/en/api/cli/beta/memory_stores/*` could not be fetched.

---

## 10. files-expert.md — `unable to verify`

**Coverage (sitemap):** Upstream has five subcommands — upload, download, retrieve-metadata, list, delete. All five are documented in the local file ✓  
**Content drift:** Unable to verify — Exa unavailable.  
**Failure cause:** Same as above. Pages at `https://platform.claude.com/docs/en/api/cli/beta/files/*` could not be fetched.

---

## Aggregate recommendation

### Files needing a follow-up edit pass

| File | Issue | Priority |
|------|-------|----------|
| `agents-expert.md` | Add `ant beta:agents delete` CLI block (lines 14–69) and update allowed-commands rule (line 175) | High — confirmed structural gap |

### Files that could not be content-verified (9)

`environments-expert.md`, `sessions-expert.md`, `events-expert.md`, `tools-expert.md`, `multiagent-expert.md`, `skills-expert.md`, `mcp-vaults-expert.md`, `memory-expert.md`, `files-expert.md`

A follow-up run with a functioning Exa tool namespace is strongly recommended to check for:

- New or changed `--beta` header values
- New flags on existing subcommands
- Changed or extended response-shape fields
- Deprecated usage examples
- New model IDs or speed modes
- New beta feature flags (e.g., research-preview gates)

### Secondary finding: docs-auditor.md whitelist

The `docs-auditor.md` coverage-mode in-scope whitelist does not include `memory_stores`. This causes the auditor to classify the entire `memory_stores` domain as out-of-scope, preventing automatic coverage checks for `memory-expert.md` in future runs. Consider adding `memory_stores` to the whitelist.

### Upstream out-of-scope FYI (not actionable for this pipeline)

- `messages` domain: 8 subcommands (create, count-tokens, batches:cancel/create/delete/list/results/retrieve)
- `models` domain: 2 subcommands (list, retrieve)
- `memory_stores` domain: 14 subcommands (covered by memory-expert.md despite whitelist classification)
