# Docs Drift Report

**Run timestamp:** 2026-04-27T16:20:59Z  
**HEAD commit:** 535dd8665fb253c88d74ba080c591816e38b105b  
**Summary:** 0 in-sync / 2 drift detected / 8 unable to verify

---

## Methodology

Coverage audit ran via `docs-auditor` in **coverage mode**: `curl -s https://platform.claude.com/sitemap.xml` → extracted all `/docs/en/api/cli/beta/` leaf URLs → diffed against `grep 'ant beta:[a-z0-9:_-]+'` across `.claude/agents/*.md`.

Per-page **section mode** and **schema mode** fetches were attempted for the two confirmed-drift files but failed: the `mcp__exa__crawling_exa` tool is not provisioned in this environment and the `get-code-context-exa` skill fallback was also unavailable. This means verbatim upstream CLI flag text cannot be quoted for any file; content-accuracy verification is marked `unable to verify` throughout. Coverage findings (new or missing subcommands) are authoritative — they derive from the raw sitemap XML, not from Exa crawls.

**Note on false positives in raw coverage output:** Local agent files document CLI commands with space-separated syntax (e.g., `ant beta:agents create`) rather than colon-separated tokens (e.g., `ant beta:agents:create`). The coverage-mode grep regex `ant beta:[a-z0-9:_-]+` only matches the domain prefix from space-separated lines, causing every individual subcommand to appear as a gap. After accounting for this format difference, the real gaps are only the two findings below.

---

## agents-expert.md

**Status:** `drift detected`

**Finding:** The upstream sitemap confirms `ant beta:agents:delete` exists at  
`https://platform.claude.com/docs/en/api/cli/beta/agents/delete`  
but `.claude/agents/agents-expert.md` contains no `delete` subcommand anywhere in its CLI Commands section (lines 14–69). The file lists `create`, `retrieve`, `update`, `list`, `archive`, and `versions list` — delete is entirely absent.

**Relevant local line range:** Lines 14–69 (the `## CLI Commands` block). A new entry for `ant beta:agents delete` must be inserted here.

**Upstream source:** `https://platform.claude.com/docs/en/api/cli/beta/agents/delete`  
Verbatim content: *unable to fetch — mcp__exa__crawling_exa not provisioned; Exa fallback unavailable.*

**Recommended fix:** Add a `ant beta:agents delete` entry to the CLI Commands block. Fetch the canonical page to obtain the exact flag list before editing.

---

## environments-expert.md

**Status:** `unable to verify`

**Coverage:** Sitemap confirms all 6 expected subcommands — `create`, `retrieve`, `update`, `list`, `archive`, `delete` — and all 6 are present in the local file (lines 14–100). No structural gap.

**Content accuracy:** Per-page flag/schema verification not possible (Exa unavailable). The `--beta string (repeatable)` flag format and all listed OPTIONS fields could not be diffed against live upstream.

---

## sessions-expert.md

**Status:** `unable to verify`

**Coverage:** Sitemap confirms all expected subcommands: `create`, `retrieve`, `update`, `list`, `archive`, `delete`, `events/send`, `events/list`, `events/stream`, `resources/add`, `resources/retrieve`, `resources/update`, `resources/list`, `resources/delete`. All are present in the local file (lines 14–97). No structural gap.

**Content accuracy:** Per-page verification not possible (Exa unavailable).

---

## events-expert.md

**Status:** `unable to verify`

**Coverage:** Sitemap confirms `events/send`, `events/list`, `events/stream` and all three are present in the local file (lines 14–35). No structural gap.

**Content accuracy:** Per-page verification not possible (Exa unavailable). Event type tables (lines 44–88), stop-reason table, and outcome/span event fields could not be diffed against live upstream.

---

## tools-expert.md

**Status:** `unable to verify`

**Coverage:** This domain has no standalone CLI subcommands — tools are configured via `--tool` flag on agent operations. The sitemap contains no `/cli/beta/tools/` entries. Coverage is consistent.

**Content accuracy:** Built-in tool list (`agent_toolset_20260401` tool names, permission policy types, `custom` tool name regex) could not be verified against live upstream (Exa unavailable).

---

## multiagent-expert.md

**Status:** `unable to verify`

**Coverage:** `callable_agents` is an API field, not a standalone CLI domain. No `/cli/beta/multiagent/` entries exist in the sitemap. Consistent with local file stating thread subcommands use REST API directly (lines 76–91).

**Content accuracy:** Thread API endpoint shapes (`GET /v1/sessions/$SESSION_ID/threads`, stream/events paths) and event type descriptions could not be diffed against live upstream (Exa unavailable).

---

## skills-expert.md

**Status:** `unable to verify`

**Coverage:** Sitemap confirms all 8 expected subcommands — `create`, `retrieve`, `list`, `delete`, `versions/create`, `versions/retrieve`, `versions/list`, `versions/delete` — and all 8 are present in the local file (lines 14–63). No structural gap.

**Content accuracy:** Per-page verification not possible (Exa unavailable). Skill response shapes and version response shapes (lines 136–175) could not be diffed.

---

## mcp-vaults-expert.md

**Status:** `unable to verify`

**Coverage:** Sitemap confirms all 12 expected subcommands — `vaults: create, retrieve, update, list, archive, delete` and `vaults/credentials: create, retrieve, update, list, archive, delete` — and all 12 are present in the local file (lines 14–188). No structural gap.

**Content accuracy:** Per-page verification not possible (Exa unavailable). Credential auth type variants (`mcp_oauth`, `static_bearer`, `token_endpoint_auth` subtypes) and response shapes could not be diffed.

---

## memory-expert.md

**Status:** `drift detected`

**Finding:** `.claude/agents/memory-expert.md` line 14 states:

> `Note: ant beta:memory-stores does not yet exist in the CLI. Use the REST API directly or pass YAML via ant beta:sessions create for attaching stores to sessions.`

However, the upstream sitemap (fetched 2026-04-27T16:20:59Z) confirms a full `memory-stores` CLI suite is now live:

| Upstream subcommand | URL |
|---|---|
| `ant beta:memory-stores:create` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/create |
| `ant beta:memory-stores:retrieve` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/retrieve |
| `ant beta:memory-stores:update` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/update |
| `ant beta:memory-stores:list` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/list |
| `ant beta:memory-stores:archive` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/archive |
| `ant beta:memory-stores:delete` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/delete |
| `ant beta:memory-stores:memories:create` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/memories/create |
| `ant beta:memory-stores:memories:retrieve` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/memories/retrieve |
| `ant beta:memory-stores:memories:update` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/memories/update |
| `ant beta:memory-stores:memories:list` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/memories/list |
| `ant beta:memory-stores:memories:delete` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/memories/delete |
| `ant beta:memory-stores:memory-versions:retrieve` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/memory_versions/retrieve |
| `ant beta:memory-stores:memory-versions:list` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/memory_versions/list |
| `ant beta:memory-stores:memory-versions:redact` | https://platform.claude.com/docs/en/api/cli/beta/memory_stores/memory_versions/redact |

**Relevant local line range:** Line 14 (the "CLI Commands" stub note). The entire `## CLI Commands` section needs to be replaced with real `ant beta:memory-stores` and `ant beta:memory-stores:memories` help output.

**Upstream source:** `https://platform.claude.com/sitemap.xml` (coverage confirmation). Per-subcommand verbatim content: *unable to fetch — mcp__exa__crawling_exa not provisioned; Exa fallback unavailable.*

**Recommended fix:** Replace the stub note at line 14 with real CLI help for all 14 subcommands listed above. The `memory-stores` domain is currently listed as out-of-scope in the coverage-mode whitelist in `docs-auditor.md` — that whitelist entry should also be updated to include `memory-stores` now that the CLI exists. Fetch each canonical page to obtain exact flag lists before editing.

---

## files-expert.md

**Status:** `unable to verify`

**Coverage:** Sitemap confirms all 5 expected subcommands — `upload`, `download`, `retrieve-metadata`, `list`, `delete` — and all 5 are present in the local file (lines 14–44). No structural gap.

**Content accuracy:** Per-page verification not possible (Exa unavailable). File response shapes and list-pagination fields (lines 165–200) could not be diffed.

---

## Aggregate Recommendation

**2 files need a follow-up edit pass:**

1. **`.claude/agents/agents-expert.md`** — Add `ant beta:agents delete` to the CLI Commands section. Fetch `https://platform.claude.com/docs/en/api/cli/beta/agents/delete` to get exact flags and add a corresponding API Reference entry (DELETE /v1/agents/{agent_id} lifecycle notes).

2. **`.claude/agents/memory-expert.md`** — Replace the "CLI does not yet exist" stub with real `ant beta:memory-stores` and `ant beta:memory-stores:memories` CLI help blocks. 14 new subcommands are now live upstream. Also update `docs-auditor.md`'s in-scope whitelist to include `memory-stores`.

**8 files could not have content accuracy verified** (environments-expert, sessions-expert, events-expert, tools-expert, multiagent-expert, skills-expert, mcp-vaults-expert, files-expert). Their subcommand coverage looks intact per the sitemap, but flag names, option descriptions, response schemas, and beta header strings were not diff-able in this run. A follow-up run with `mcp__exa__crawling_exa` provisioned is recommended for a full content audit.

**Action required to unblock content-accuracy verification:** Ensure the `mcp__exa__crawling_exa` tool is provisioned in the `docs-auditor` agent definition (it is listed in the frontmatter `tools:` field but was not available at runtime in this environment).
